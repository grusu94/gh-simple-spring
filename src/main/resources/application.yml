spring:
  application:
    name: gh-simple-spring
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:default}
  config:
    import: ${SPRING_CONFIG_IMPORT:default}

server:
  port: 9000

simple:
  greeting:
    message: ${greeting.message}
